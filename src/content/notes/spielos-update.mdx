---
title: 3 Things Shipped to Spiel OS This Week
date: 2026-06-18 09:00:00 +0000
description: Spiel OS is a pipeline that turns a build session into a publishable
  post. The gap that kept showing up was not the writing. It was the publishing, the
  last 20% that takes 80% of the energy.
permalink: /spielos-update/
category: journey
tags:
- Founder Distribution
- spielos
- product-update
- building-in-public
image: /assets/og/spielos-update.png
---

import SectionHead from "@/components/posts/SectionHead.astro";
import ProseCenter from "@/components/posts/ProseCenter.astro";
import Card from "@/components/posts/Card.astro";
import Pullquote from "@/components/posts/Pullquote.astro";
import Takeaway from "@/components/posts/Takeaway.astro";
import Source from "@/components/posts/Source.astro";
import PostCta from "@/components/posts/PostCta.astro";



<SectionHead tag='The trap' title='The Last 20%' sub='Every pipeline that turns building into publishing eventually breaks at the same point. The writing is solved. The dispatch is not.' />

<ProseCenter text='You build something. The session gets captured. The transformer drafts the post. The draft lands in your queue. You approve. Then you open three browser tabs and start copy-pasting.' />

<Takeaway text='The friction was never the writing. It was everything after the writing.' />

<hr class='divider' />

<section class='section'>
  <SectionHead tag='The ships' title='What Shipped This Week' sub='Three things that remove steps instead of adding power.' />

  <div class='grid grid-3'>
    <Card title='1: One API call' desc='Buffer integration. X, LinkedIn, Threads in a single call. Free tier: 3 channels, 10 queued posts, 3,000 API calls a month.' icon='zap' num='1' variant='stat-shift' />
    <Card title='2: Custom banners' desc='Config + HTML template, opens in your browser. No Node, no Puppeteer. Edit a CSS variable, hit refresh, ship.' icon='image' num='2' variant='stat-shift' />
    <Card title='3: 5 strategy docs' desc='Frontmatter, version history, status markers, supersedes lineage. Voice-matched output starts from a clean input contract.' icon='layers' num='3' variant='stat-shift' />
  </div>
</section>

<hr class='divider' />

<section class='section'>
  <SectionHead tag='Ship 1' title='One API Call, Three Platforms' sub='Buffer integration that falls back to direct APIs when you outgrow the free tier.' />

  <ProseCenter text='The Buffer integration posts to X, LinkedIn, and Threads in a single call. No copy-paste between tabs. No three separate logins. No scheduling tool with its own auth flow.' />

  <ProseCenter text={'It is built on Buffer\u0027s multi-platform API. The free tier is genuinely generous: 3 channels, 10 posts in queue, 3,000 API calls a month. One post a day across three platforms with headroom.'} />

  <div class='grid grid-2'>
    <Card title='Free tier: 3 channels · 10 queued · 3,000 calls/mo' desc='One post a day, three platforms, with headroom. No credit card to start.' icon='check' />
    <Card title='Outgrow path: Direct X and LinkedIn APIs' desc='When you outgrow the free tier, the tool falls back automatically. No config change, no tool switch.' icon='zap' />
  </div>

  <Takeaway text='The marginal cost from shipped a build to published across three platforms is now near zero.' />
</section>

<hr class='divider' />

<section class='section'>
  <SectionHead tag='Ship 2' title='Banners Without the Node Tax' sub='Open one command. Edit a CSS variable. Hit refresh. The visual renders.' />

  <ProseCenter text='The banner tool generates post visuals from a config file and an HTML template. Open one command. A real HTML file opens in your browser. Edit a CSS variable. Hit refresh. See the new banner.' />

  <ProseCenter text='No Node setup. No Puppeteer. No Chrome path fixes. No shell-out dance. The engine is Playwright + system Chrome. The templates use CSS variables in a :root block. Change a color, save, render.' />

  <div class='grid grid-2'>
    <Card title='Test coverage: 14 unit tests + 1 snapshot' desc='If a banner drifts more than 2% pixel-wise, the test fails. Drift is caught before it ships.' icon='check' />
    <Card title='Excuse remover: No more no banner excuse' desc='A post without a banner looks half-finished. The lack of a visual is the most common reason builders skip posting.' icon='check' />
  </div>

  <Takeaway text='The tool removes the most common excuse for not posting.' />
</section>

<hr class='divider' />

<section class='section'>
  <SectionHead tag='Ship 3' title='Five Strategy Docs, Voice Matched' sub='The pipeline reads these to write in your voice. Drift in, drift out.' />

  <ProseCenter text='The docs the pipeline reads to write in your voice got a structural rewrite. Clean frontmatter. Version history. Status markers. Supersedes lineage. A clear source-of-truth declaration for every concept.' />

  <Pullquote text='When the docs are clean, the output is clean. When they drift, the output drifts. The polish is not cosmetic. It is the input contract for the entire pipeline.' cite='The builder never stops building. The builder never writes a post from scratch.' />

  <ProseCenter text='The five docs are the difference between an AI that sounds like you and an AI that sounds like an AI. They are not optional. They are the moat.' />
</section>

<hr class='divider' />

<section class='section'>
  <SectionHead tag='The lesson' title='Removing Friction Beats Adding Features' sub='These 3 ships do not add power. They remove steps. That is the whole point.' />

  <ProseCenter text='The week lesson: removing friction from publishing is worth more than adding another feature. When the marginal cost of publish is near zero, the only question left is what is worth saying.' />

  <ProseCenter text='The constraint shifts from effort to taste. The pipeline handles the dispatch. You stay the editor. The builder never stops being the builder.' />
</section>

<hr class='divider' />

<section class='section'>
  <SectionHead tag='How to try' title='Clone, Setup, Ship' sub='The repo is open source. The setup is a 14-question prompt.' />

  <ProseCenter text='Clone the repo. Drop the setup prompt into any LLM agent. Walk through the 14-question setup. The five strategy documents get filled with your strategy. Then every build session becomes:' />

  <div class='step-list'>
    <div class='step-item'>
      <div class='step-num'>01</div>
      <div class='step-body'>
        <strong>Session log → 2 minutes of capture</strong>
        What was decided, what was traded off, what was learned. Structured, searchable.
      </div>
    </div>
    <div class='step-item'>
      <div class='step-num'>02</div>
      <div class='step-body'>
        <strong>Pipeline compiles, drafts, gates</strong>
        The 8-step compiler runs. The 16 mechanical gates fire. The 14-gate creative test runs. Composite score must hit 0.85.
      </div>
    </div>
    <div class='step-item'>
      <div class='step-num'>03</div>
      <div class='step-body'>
        <strong>Drafts land in queue</strong>
        X, LinkedIn, blog. Each drafted in your voice from the strategy docs. Banner rendered. All queued for review.
      </div>
    </div>
    <div class='step-item'>
      <div class='step-num'>04</div>
      <div class='step-body'>
        <strong>You review, publish, keep building</strong>
        One command dispatches to all three platforms. The marginal cost is near zero. You stay a builder.
      </div>
    </div>
  </div>

  <ProseCenter text='No editorial calendar. No what should I post loops. No blank pages. You build. The pipeline posts.' />
</section>

<PostCta takeaway='You build. The pipeline posts. You stay a builder.' echo='Three ships this week. One repo. Zero excuses. Run the setup and reply with what you ship.' btnText='Clone the SpielOS repo' btnHref='https://github.com/ShayanSpiel/SpielOS' />
