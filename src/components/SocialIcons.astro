---
import { SOCIAL, AUTHOR } from "../config";

export interface Props {
  size?: "sm" | "md";
  show?: ("x" | "linkedin" | "github" | "rss" | "email")[];
}

const { size = "sm", show = ["x", "linkedin", "github"] } = Astro.props;
const iconClass = size === "sm" ? "icon-base" : "icon-lg";
---

{show.includes("x") && (
  <a href={SOCIAL.x} target="_blank" rel="noopener noreferrer me" class="text-muted-foreground hover:text-foreground transition-colors" aria-label="X"><i class={`bx bxl-twitter ${iconClass}`} aria-hidden="true"></i></a>
)}
{show.includes("linkedin") && (
  <a href={SOCIAL.linkedin} target="_blank" rel="noopener noreferrer me" class="text-muted-foreground hover:text-foreground transition-colors" aria-label="LinkedIn"><i class={`bx bxl-linkedin-square ${iconClass}`} aria-hidden="true"></i></a>
)}
{show.includes("github") && (
  <a href={SOCIAL.github} target="_blank" rel="noopener noreferrer me" class="text-muted-foreground hover:text-foreground transition-colors" aria-label="GitHub"><i class={`bx bxl-github ${iconClass}`} aria-hidden="true"></i></a>
)}
{show.includes("rss") && (
  <a href="/feed.xml" class="text-muted-foreground hover:text-foreground transition-colors" aria-label="RSS">
    <i class="bx bx-rss icon-sm"></i>
  </a>
)}
{show.includes("email") && (
  <a href={`mailto:${AUTHOR.email}`} class="text-muted-foreground hover:text-foreground transition-colors" aria-label="Email">
    <i class="bx bx-envelope icon-sm"></i>
  </a>
)}
