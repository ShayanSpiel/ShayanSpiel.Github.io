# Production build env for the chat assistant (goal-3220ae01c4f7).
# The function URL is public by design: every visitor's browser calls it
# anonymously (verify_jwt=false); access control is the function's CORS
# allowlist + rate limits. Astro loads this file automatically on `astro build`.
PUBLIC_CHAT_FUNCTION_URL=https://vufrberqvpbqesufrymn.supabase.co/functions/v1/chat
