---
description: Approve and start the single proposed next run for a completed SpielOS run
agent: director
---

Inspect the completed report and proposed experiment for `$ARGUMENTS`. Explain
the one variable that changes, what remains fixed, expected evidence, external
actions, and approval points. Treat this command as the user's approval to
create the proposed next run, but never as approval for its later live action.
Run `company next` and report where the new run parks.
