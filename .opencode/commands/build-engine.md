---
description: Define and commission a new SpielOS business engine through a bounded system-improvement goal
agent: director
---

Treat `$ARGUMENTS` as a requested business capability, not permission to code
immediately. Define its measurable purpose, supported goal metrics,
configuration contract, external actions, approval points, evidence sources,
evaluation behavior, allowed files, and acceptance tests. Create a bounded
`system_improvement` goal with `change_kind: create_engine`,
`from_version: new`, and a complete `engine_spec`. Present the task for approval;
after approval, delegate implementation to the system-improvement agent.
