---
description: Record acceptance results for a bounded system-improvement task
agent: director
---

Inspect the change task in `$ARGUMENTS`. Confirm every changed file is allowed
and every acceptance command actually passed before recording completion.
Version or deploy only when supported by persisted test evidence.
