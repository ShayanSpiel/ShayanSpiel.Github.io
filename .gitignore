# Astro build output
dist/
.astro/

# Node
node_modules/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*~

# Logs
*.log

# Company runtime: credentials, private inputs, state, and generated work stay local
.spielos/.env
.spielos/data/
.spielos/state/
.spielos/artifacts/

# Python bytecode + runtime artifacts
**/__pycache__/
*.pyc
*.pid
*.out

# Company runtime DB + internal agent state — local only, never publish
.spielos/*.sqlite
.spielos/*.sqlite-*
.agents/.spielos/

# Company runtime SQLite DBs — local only
.spielos/*.db
.spielos/*.db-*

# Stray harness DBs at .agents root — local only
.agents/*.db
.agents/*.sqlite
.agents/*.sqlite-*
.agents/*.db-*

# Python test cache
.pytest_cache/

# Harness user-layer overrides (identity, brand, private config)
.agents/company/config.user.json

# Packaging build artifacts
.agents/build/
.agents/*.egg-info/
dist-build/

.spielos/.env.*
